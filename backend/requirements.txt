# Pinned to the exact versions verified working by the end-to-end smoke test
# (signup -> morning check-in -> evening -> weekly review, all green on Groq).
# Linux-safe: psycopg[binary] + psycopg2-binary ship prebuilt wheels (no libpq needed).

fastapi==0.138.1
uvicorn[standard]==0.49.0

# --- data layer ---
SQLAlchemy==2.0.51
alembic==1.18.5
psycopg2-binary==2.9.12        # SQLAlchemy postgresql:// driver (app tables)

# --- validation / settings ---
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0

# --- auth (JWT) ---
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==4.0.1                  # passlib 1.7.4 reads bcrypt.__about__; >=4.1 removed it
python-multipart==0.0.32

# --- agent core (LangGraph + Groq) ---
langgraph==1.2.6
langchain-core==1.4.8
langchain-groq==1.1.3
langgraph-checkpoint-sqlite==3.1.0
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.3.4         # LangGraph PostgresSaver driver (checkpointer)

# --- scheduling / misc ---
apscheduler==3.11.2
python-dotenv==1.2.2
httpx==0.28.1
